<Window x:Class="FileCloner.Views.RequesterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileCloner.Views"
        mc:Ignorable="d"
        Title="Requester View" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select files to clone:" FontWeight="Bold" Margin="10"/>
            <ItemsControl ItemsSource="{Binding FilesToClone}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding FileName}" Margin="5"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Content="Start Cloning" Command="{Binding StartCloningCommand}" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
