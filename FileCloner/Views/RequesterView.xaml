<Window x:Class="FileCloner.Views.RequesterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileCloner.Views"
        mc:Ignorable="d"
        Title="Requester View" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select Files to Clone:" FontSize="16" Margin="10"/>

            <!-- File List -->
            <ListView ItemsSource="{Binding Files}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding FileName}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Start Cloning Button -->
            <Button Content="Start Cloning" Command="{Binding StartCloningCommand}" 
                    Width="150" Height="40" HorizontalAlignment="Center" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
